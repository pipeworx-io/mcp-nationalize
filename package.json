{
  "name": "@pipeworx/mcp-nationalize",
  "version": "0.1.0",
  "description": "MCP server for nationality prediction from names via Nationalize.io API",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "keywords": ["mcp", "mcp-server", "model-context-protocol", "pipeworx", "nationalize", "nationality", "names"],
  "author": "Pipeworx",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
